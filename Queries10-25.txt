//Number of Trainings Requested, for a given SPOC Name
  SELECT COUNT(TRF.TRF_ID)
  FROM TRAINING_REQUEST_FORM TRF
  JOIN VERTICAL_TYPE VT
  ON TRF.VER_ID = VT.VER_ID
  JOIN LD_TEAM LT
  ON VT.VER_ID = LT.LD_VER_REP
  WHERE LT.LD_USER_NAME = 'CLINTON EXODIA'
	AND TRF.TRF_DATE_REQUESTED > to_date('30-09-2018', 'dd-MM-yyyy')
	AND TRF.TRF_DATE_REQUESTED < to_date('01-11-2018', 'dd-MM-yyyy');

//Number of Trainings Requested, grouped by SPOC Name
    SELECT LT.LD_USER_NAME, COUNT(TRF.TRF_ID)
  FROM TRAINING_REQUEST_FORM TRF
  JOIN VERTICAL_TYPE VT
  ON TRF.VER_ID = VT.VER_ID
  JOIN LD_TEAM LT
  ON VT.VER_ID = LT.LD_VER_REP
  WHERE TRF.TRF_DATE_REQUESTED > to_date('30-09-2018', 'dd-MM-yyyy')
	AND TRF.TRF_DATE_REQUESTED < to_date('01-11-2018', 'dd-MM-yyyy')
  GROUP BY LT.LD_USER_NAME;

//Number of Completed Trainings, for a given SPOC Name
SELECT COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  JOIN TRAINING_REQUEST_FORM TRF
  ON CT.TRF_ID = TRF.TRF_ID
  JOIN VERTICAL_TYPE VT
  ON TRF.VER_ID = VT.VER_ID
  JOIN LD_TEAM LT
  ON VT.VER_ID = LT.LD_VER_REP
  WHERE LT.LD_USER_NAME = 'CLINTON EXODIA'
  AND CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy');

//Number of Completed Trainings, grouped by SPOC Name
SELECT LT.LD_USER_NAME, COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  JOIN TRAINING_REQUEST_FORM TRF
  ON CT.TRF_ID = TRF.TRF_ID
  JOIN VERTICAL_TYPE VT
  ON TRF.VER_ID = VT.VER_ID
  JOIN LD_TEAM LT
  ON VT.VER_ID = LT.LD_VER_REP
  WHERE CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy')
  GROUP BY LT.LD_USER_NAME;


//Number of Completed Internal Trainings, for a given SPOC Name
SELECT COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  JOIN TRAINING_REQUEST_FORM TRF
  ON CT.TRF_ID = TRF.TRF_ID
  JOIN VERTICAL_TYPE VT
  ON TRF.VER_ID = VT.VER_ID
  JOIN LD_TEAM LT
  ON VT.VER_ID = LT.LD_VER_REP
  WHERE LT.LD_USER_NAME = 'CLINTON EXODIA'
  AND CT.CT_TRAINING_SOURCE = '1'
  AND CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy');

//Number of Completed Development Trainings, for a given SPOC Name
SELECT COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  JOIN TRAINING_REQUEST_FORM TRF
  ON CT.TRF_ID = TRF.TRF_ID
  JOIN VERTICAL_TYPE VT
  ON TRF.VER_ID = VT.VER_ID
  JOIN LD_TEAM LT
  ON VT.VER_ID = LT.LD_VER_REP
  WHERE LT.LD_USER_NAME = 'CLINTON EXODIA'
  AND CT.CT_TRAINING_SOURCE = '2'
  AND CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy');

//Number of Completed Vendor Trainings, for a given SPOC Name
SELECT COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  JOIN TRAINING_REQUEST_FORM TRF
  ON CT.TRF_ID = TRF.TRF_ID
  JOIN VERTICAL_TYPE VT
  ON TRF.VER_ID = VT.VER_ID
  JOIN LD_TEAM LT
  ON VT.VER_ID = LT.LD_VER_REP
  WHERE LT.LD_USER_NAME = 'CLINTON EXODIA'
  AND CT.CT_TRAINING_SOURCE = '3'
  AND CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy');



//Number of Trainings Requested, for all verticals
SELECT COUNT(TRF.TRF_ID)
  FROM TRAINING_REQUEST_FORM TRF
  WHERE TRF.TRF_DATE_REQUESTED > to_date('30-09-2018', 'dd-MM-yyyy')
  AND TRF.TRF_DATE_REQUESTED < to_date('01-11-2018', 'dd-MM-yyyy');

//Number of Completed Trainings, for all verticals
SELECT COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  WHERE CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy');

//Number of Completed Internal Trainings, for all verticals
SELECT COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  WHERE CT.CT_TRAINING_SOURCE = '1' 
  AND CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy');

//Number of Completed Development Trainings, for all verticals
SELECT COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  WHERE CT.CT_TRAINING_SOURCE = '2' 
  AND CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy');

//Number of Completed Vendor Trainings, for a given SPOC Name
SELECT COUNT(CT.TRF_ID)
  FROM CONFIRMED_TRAINING CT
  WHERE CT.CT_TRAINING_SOURCE = '3' 
  AND CT.CT_PROPOSED_END_DATE < sysdate
  AND CT.CT_PROPOSED_END_DATE > to_date('30-09-2018', 'dd-MM-yyyy')
  AND CT.CT_PROPOSED_END_DATE < to_date('01-11-2018', 'dd-MM-yyyy');


